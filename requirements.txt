# ── Ollama Model Scheduler — Python dependencies ──────────────────────────────
#
# Install with:
#   pip install -r requirements.txt
#
# Tested on Python 3.11 / 3.12 on Ubuntu 22.04 (g5.2xlarge AMI).

# Web framework
fastapi>=0.111.0

# ASGI server — uvloop for maximum async throughput on Linux
uvicorn[standard]>=0.29.0
uvloop>=0.19.0            # C-based event loop; ~2× faster than asyncio default

# Async HTTP client (connection pooling, streaming)
httpx>=0.27.0

# Settings / validation
pydantic>=2.7.0
pydantic-settings>=2.2.0
